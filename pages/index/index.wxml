<!--index.wxml-->
<view class="container">
    <!-- <v-articleList article-list="{{ ArticleList }}" more="{{ getMore }}" mId="{{ mId }}"/> -->
    <v-nav />
    <v-recommend recommend="{{ RecommendInfo }}"/>
</view>
